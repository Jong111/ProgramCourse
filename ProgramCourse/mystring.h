//#pragma once
//extern char* str_cat(char* dest, char* src);
//extern int str_len(char* str);
//extern char* str_cpy(char* dest, char* src, int len);
//extern int str_cmp(char* str1, char* str2);
//extern char* str_replace(char* dest, char* src1, char* src2);
//extern int str_find(char* str1, char* str2);
//extern char* str_insert(char* dest, int index, char* src);
//extern char* to_lower(char* str);
//
//// The following two extra functions are used simplify some processes in above functions
//extern int in(int a, int* arr, int size);
//extern void str_findSeq(char* str1, char* str2, int* res);
//
//
